from Player import Player


class Dealer(Player):
    """
    Dealer is a special case of player
    differs:
        -starts with 1 card shown one hidden
        -hits if score < 17
            else stand
    """
    pass